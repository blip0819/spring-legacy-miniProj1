<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosa.proj.board.mapper.BoardMapper">

    <select id="list" resultType="org.kosa.proj.board.BoardVO">
       select 
			b.*, m.memberName bwriter  
		from TB_BOARD b inner join tb_member m on b.memberID = m.memberID
		order by bno;
    </select>

	<select id="view" resultType="org.kosa.proj.board.BoardVO">
		select 
			b.*, m.memberName bwriter  
		from TB_BOARD b inner join tb_member m on b.memberID = m.memberID  
		where bno = #{bno}
	</select>
	
	<delete id="delete">
		delete 
		from TB_BOARD 
		where bno = #{bno}
	</delete>
	
	<delete id="clear" >
		delete 
		from TB_BOARD 
	</delete>
	
	<update id="update">
		update
			TB_BOARD
		set
			btitle = #{btitle}
			bcontent = #{bcontent} 
		where bno = #{bno}
	</update>
	
	<insert id="insert" >
		insert into TB_BOARD (
			btitle,
			bcontent,
			memberID
		) values (
			#{btitle},
			#{bcontent},
			#{memberID}
		) 
	</insert>
    
</mapper>
